interface:
  display_name: "Devops Sst"
  short_description: "Repo-specific patterns and workflows for devops-sst"
  default_prompt: "Use the devops-sst repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true